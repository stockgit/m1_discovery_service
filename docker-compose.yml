version: '3.8'
services:
  api-demo:
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    ports:
      # Access http://localhost/
      - 8061:8061
    environment:
      SPRING_PROFILES_ACTIVE: docker